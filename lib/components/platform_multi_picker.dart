import 'package:flutter/material.dart';

class PlatformMultiPicker extends StatelessWidget {
  const PlatformMultiPicker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
